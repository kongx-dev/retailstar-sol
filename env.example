# Frontend Environment Variables (VITE_ prefix required for Vite)
# Copy this file to .env.local and add your actual credentials

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Supabase Auth Configuration
# Enable these OAuth providers in your Supabase dashboard:
# - Google OAuth (Google Cloud Console)
# - Twitter OAuth (Twitter Developer Portal) 
# - Discord OAuth (Discord Developer Portal)
# Set redirect URLs to: https://yourdomain.com/outerring

# Solana Configuration
VITE_SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
VITE_SOLANA_NETWORK=mainnet-beta

# Add other frontend-specific variables as needed
VITE_API_URL=https://api.retailstar.com
VITE_APP_ENV=development
